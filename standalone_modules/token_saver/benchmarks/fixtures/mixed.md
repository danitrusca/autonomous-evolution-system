Here is some text that basically, actually, simply explains the concept. In fact, it should be concise.

Inline code like `doThing()` must remain unchanged, even if we really want to adjust it.

```js
// Fenced code must remain unchanged
function doThing() {
  console.log("Hello");
}
```

